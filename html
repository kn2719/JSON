<!DOCTYPE html>
<html>
<head>
    <title>JSON Data</title>
    <style>
        /* Add CSS styles for a visually appealing UI */
    </style>
</head>
<body>
    <h1>JSON Data</h1>

    <!-- Search form -->
    <form method="POST">
        <input type="text" name="search_name" placeholder="Search by name">
        <select name="filter_designation">
            <option value="">All</option>
            <option value="Software Engineer">Software Engineer</option>
            <option value="UI/UX Designer">UI/UX Designer</option>
            <!-- Add more options as needed -->
        </select>
        <button type="submit">Filter</button>
    </form>

    <!-- Display filtered JSON data -->
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Age</th>
                <th>Email</th>
                <th>Designation</th>
                <th>Skills</th>
                <!-- Add more fields as needed -->
            </tr>
        </thead>
        <tbody>
            {% for data in data %}
            <tr>
                <td>{{ data.name }}</td>
                <td>{{ data.age }}</td>
                <td>{{ data.email }}</td>
                <td>{{ data.designation }}</td>
                <td>{{ ', '.join(data.skills) }}</td>
                <!-- Display more fields as needed -->
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
